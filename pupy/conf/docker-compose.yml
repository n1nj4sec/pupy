version: '3'

services:
  pupy:
    image: alxchk/pupy:base-${TAG}
    user: ${UID}:${GID}
    volumes:
      - ${PUPY}:/opt/pupy
      - ${WORKDIR}:/project
    ports:
      - 8443:8443
    container_name: pupy
    stdin_open: true
    tty: true
